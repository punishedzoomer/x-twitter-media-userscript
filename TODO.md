# X Media Gallery Userscript - Task List

- [x] **Remove Sync Timeline Button**: Remove the "Sync Native Timeline to Here" button from the lightbox viewer, leaving only the "View Original Tweet" button.
- [x] **Lightbox Navigation**: Add Left (`<`) and Right (`>`) navigation arrows to the full-screen lightbox to allow smooth cycling through the media grid without closing it, matching X's native media viewing experience.
- [x] **Redesign Close Button**: Modify the red "Close" button on the popup gallery header to align with X's minimalist design philosophy (e.g., a simple circular button with an 'X' icon or clean text without the jarring red background).
- [x] **Fix Bookmark Button**: Removed the bookmarkBtn element and its logic from the lightbox due to recurring GraphQL failures; investigation needed on current mutation payload requirements.
